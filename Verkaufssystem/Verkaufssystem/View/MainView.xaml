<Window x:Class="Verkaufssystem.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Verkaufssystem.View"
        xmlns:vm="clr-namespace:Verkaufssystem.ViewModel"
        mc:Ignorable="d"
        Title="SneakerMovement" Height="723.64" Width="1134.4">
    <Window.DataContext>
        <vm:MainViewModel></vm:MainViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Margin="5" Command="{Binding OpenChildViewCommand}" Content="Open Child Window" HorizontalAlignment="Left"/>
            <Button Margin="5" Command="{Binding UpdateViewCommand}" CommandParameter="Home" Content="Show Home View"/>
            <Button Margin="5" Command="{Binding UpdateViewCommand}" CommandParameter="List" Content="Show List View"/>
        </StackPanel>
        <ContentControl Grid.Row="1">

        </ContentControl>
    </Grid>
</Window>